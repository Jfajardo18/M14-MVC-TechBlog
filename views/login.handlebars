<style>
    main {
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .col {
        max-width: 500px; /* Ensures the form doesn't stretch too wide */
        width: 100%;
        background: #ffffff;
        padding: 2rem;
        border-radius: 8px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
        text-align: center; /* Centers the text */
    }

    h2.login {
        color: #333;
        margin-bottom: 1.5rem;
    }

    .form-input {
        width: 100%;
        padding: 0.75rem;
        margin-bottom: 1rem;
        border: 1px solid #d1d5db; /* A soft gray border */
        border-radius: 4px;
        font-size: 1rem;
    }

    .form-input:focus {
        border-color: #4a90e2; /* Highlight color when input is focused */
        outline: none;
        box-shadow: 0 0 0 1px #4a90e2; /* Subtle shadow to match focus color */
    }

    .btn-primary {
        width: 100%;
        padding: 0.75rem;
        border: none;
        border-radius: 4px;
        background-color: #4a90e2; /* A pleasant, soft blue */
        color: #ffffff;
        cursor: pointer;
        font-size: 1rem;
        margin-bottom: 1rem;
    }

    .btn-primary:hover {
        background-color: #357ab8; /* Slightly darker blue on hover */
    }

    a {
        color: #4a90e2; /* Match the button color for consistency */
        text-decoration: none; /* No underline to keep it clean */
    }

    a:hover {
        text-decoration: underline; /* Underline on hover for usability */
    }
</style>


<div class="row">
  <div class="col justify-content-center">
    <h2 class="login">Login</h2>
    <form class="form login-form">
      <div class="form-group">
        <input class="form-input" type="text" placeholder="Username" id="username-login" />
      </div>
      <div class="form-group">
        <input class="form-input" type="password" placeholder="Password" id="password-login" />
      </div>
      <div class="form-group">
        <button class="btn btn-primary" type="submit">Login</button>
      </div>
      <div>
        <a href="/signUp">Sign Up Instead</a>
      </div>
    </form>
  </div>
</div>

<script src="./js/login.js"></script>